<h1>User info</h1>
<!--form with部分-->
<%= form_with model: User.new do |f| %>
  <h4>IMAGE</h4>
  <%= f.field :image, accept: "image/*" %>
  
  <h4>name</h4>
  <%= f.text_field :name %>

  <h4>introdaction</h4>
  <%= f.text_area :introdaction %>

  <%= f.submit '投稿' %>
<% end %>