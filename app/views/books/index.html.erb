<!--User infoとNew Book-->
<%= render 'book', books: @books %>

<h1>Books</h1>

<% @books.each do |book| %>

  <p>Title</p>
  <span><%= book.title %></span>
  
  <p>Opinion</p>
  <span><%= book.body %></span>
  
<% end %>