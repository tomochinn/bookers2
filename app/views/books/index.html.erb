<div class="d-flex flex-wrap">

<!--User infoとNew Book-->
<%= render 'book', books: @books %>

<h2>Books</h2>

<% @books.each do |book| %>

  <p>Title</p>
  <%= link_to book.title, book_path(book.id) %>
  
  <p>Opinion</p>
  <span><%= book.body %></span>
  
<% end %>
</div>